export interface Column {
    heading: string | JSX.Element
    value: string
    style: string
}

export interface IDetailOrder {
    name: string
    image: string
    quantity: number
    cashout: string
    price: string
    total: string
}
